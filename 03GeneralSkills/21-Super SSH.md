**Descripción:**
Using a Secure Shell (SSH) is going to be pretty important.
Can you `ssh` as `ctf-player` to `titan.picoctf.net` at port `59128` to get the flag?

You'll also need the password `f3b61b38`. If asked, accept the fingerprint with `yes`.

If your device doesn't have a shell, you can use:

**Solución1:**
![[Pasted image 20260826101420.png]]
usando ssh me conecto al servidor con el respectivo puerto, luego pongo la contraseña y ya sale la bandera
picoCTF{s3cur3_c0nn3ct10n_3e293eea}


**Notas adicionales:**
- ssh permite hacer login remoto a un server
**Referencias:**